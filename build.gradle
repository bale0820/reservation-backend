plugins {
    id 'java'
    id 'org.springframework.boot' version '3.4.8-SNAPSHOT'
    id 'io.spring.dependency-management' version '1.1.7'
}

group = 'com.example'
version = '0.0.1-SNAPSHOT'

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}

repositories {
    mavenCentral()
    maven { url = 'https://repo.spring.io/snapshot' }
}

dependencies {
    // 🌐 기본 웹 서버 (내장 톰캣 + Spring MVC + JSON 처리)
    implementation 'org.springframework.boot:spring-boot-starter-web'

// 🗄️ MyBatis - SQL 직접 작성용 (복잡한 조회, 통계, 조인 최적화)
    implementation 'org.mybatis.spring.boot:mybatis-spring-boot-starter:3.0.4'


// 🔐 Spring Security - 인증 / 인가, 필터 체인 기반 보안 처리
    implementation 'org.springframework.boot:spring-boot-starter-security'

// 🔑 JWT 라이브러리 (API + 구현체 분리 구조)
// - api: 토큰 생성/검증 인터페이스 (컴파일 시 필요)
// - impl, jackson: 실제 암호화·직렬화 로직 (실행 시 필요)
    implementation 'io.jsonwebtoken:jjwt-api:0.11.5'
    runtimeOnly 'io.jsonwebtoken:jjwt-impl:0.11.5'
    runtimeOnly 'io.jsonwebtoken:jjwt-jackson:0.11.5'


// 🧩 Spring Data JPA - ORM 기반 CRUD, 트랜잭션 관리
    implementation 'org.springframework.boot:spring-boot-starter-data-jpa'

//// 🐬 MySQL JDBC 드라이버 (실행 시 DB 연결용)
//    runtimeOnly 'com.mysql:mysql-connector-j'
// ✅ Oracle JDBC (Spring Boot 3 / Java 21 호환)
    runtimeOnly 'com.oracle.database.jdbc:ojdbc11'

// ☁ AWS SDK BOM - AWS 라이브러리 버전 충돌 방지
    implementation platform('software.amazon.awssdk:bom:2.25.28')

// ☁ AWS S3 - 이미지 / 파일 업로드 및 다운로드
    implementation 'software.amazon.awssdk:s3'


// 🤖 OpenAI API 클라이언트 (AI 분석, 텍스트 생성 등)
    implementation 'com.theokanning.openai-gpt3-java:service:0.18.2'


// 🛠 Lombok - getter/setter, 생성자 자동 생성 (컴파일 타임 전용)
    compileOnly 'org.projectlombok:lombok'
    annotationProcessor 'org.projectlombok:lombok'


// 🔐 OAuth2 Client - Google / Kakao 소셜 로그인
    implementation 'org.springframework.boot:spring-boot-starter-oauth2-client'


// 🖼 Google Vision API - 이미지 분석 (OCR, 객체 인식 등)
    implementation 'com.google.cloud:google-cloud-vision:3.35.0'


// 🧪 테스트 환경 (JUnit, Spring Test)
    testImplementation 'org.springframework.boot:spring-boot-starter-test'

// 🧪 MyBatis 테스트 전용 설정
    testImplementation 'org.mybatis.spring.boot:mybatis-spring-boot-starter-test:3.0.4'

// 🧪 JUnit 플랫폼 런처 (IDE / 빌드 도구 실행 지원)
    testRuntimeOnly 'org.junit.platform:junit-platform-launcher'


// 🔄 DevTools - 로컬 개발 시 자동 재시작, 캐시 비활성화
    developmentOnly 'org.springframework.boot:spring-boot-devtools'


//    runtimeOnly 'com.h2database:h2'
//    implementation 'mysql:mysql-connector-java:8.0.33'
}

//tasks.named('test') {
//	useJUnitPlatform()
//}
tasks.withType(Test) {
    enabled = false
}